\documentclass[../CryptoHW3.tex]{subfiles}

\begin{document}
\begin{flushleft}



\numbpr{9}
\prob{9} The \textbf{DDH} Assumption allows two parties communicating over an encrypted channel to derive a shared value $k$ (\emph{usually for use as a key}) so that, to an eavesdropper, $k$ is indistinguishable from a uniform element of some group $\mathbb{G}$.  



























\end{flushleft}
\end{document}
